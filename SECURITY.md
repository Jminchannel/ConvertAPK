# 安全政策

ConvertAPK 涉及文件上传、构建任务、产物下载、管理端鉴权和第三方密钥配置。请不要在公开 Issue 中披露可被利用的漏洞细节、真实密钥、服务器地址、管理账号或用户数据。

## 报告安全问题

如果你发现安全问题，请优先通过私下渠道联系维护者，并提供以下信息：

- 受影响的模块或接口。
- 可复现步骤。
- 可能的影响范围。
- 是否涉及真实密钥、用户数据、构建产物或管理端权限。
- 建议的修复方向。

如果暂时没有私下联系方式，可以先在 Issue 中提交不含攻击细节的安全提示，请求维护者建立私下沟通。

## 重点安全边界

- 上传接口必须校验文件类型、后缀、大小和异常分支。
- 下载和文件读取接口必须做路径约束，防止目录穿越。
- 管理端鉴权、JWT 和 `X-Client-Token` 不可绕过。
- 构建日志和错误响应不得泄露真实密钥、Token、数据库连接串或服务器路径。
- 不得提交 `.env`、证书、签名文件、keystore、生产数据库账号或第三方 API Key。
- 生产环境必须修改默认管理员密码、JWT 密钥、客户端 Token 和数据库密码。

## 合法使用

本项目只适用于合法授权的应用打包、学习研究和内部工具场景。禁止将本项目用于钓鱼、诈骗、木马、病毒、后门、盗版、仿冒、绕过平台审核或其他违法违规用途。
